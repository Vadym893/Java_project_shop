package pl.pasechnik.ecommerce.sales;
import org.junit.jupiter.api.Test;
/*
import static org.asertj.core.api.Assertions.*
public class OfferAcceptTest{
    @Test
    void itAllowsToAcceptOffer(){
        SalesFacaded sales = thereIsSales();
        String customerId = thereISCustomer("Kuba");
        String productId = thereIsProduct("smth",BigDecimal.valueOf(10))

        sales.addProduct(customerId,productId)
        var  acceptOffer:acceptOfferRequest = new acceptOfferRequest().setEmail("smth").setFname("John").setLname("Doe")
        ResercationDetails reservationDetails = sales.AcceptOffer(customerId,acceptOffer)

        assertThat(Collections.emptyList())

        assertThat(reservationDetails.getPayment()).isNotBlank()
        assertThat(reservationDetails.getReservationId()).isNotBlank()
        asserPaymentHasBeenRegistered();
        assertReservationTotalMatch(reservationDetails.getReservationId(),BigDecimal.valueOf(20))
        assertReservationTotalMatch(reservationDetails.getReservationId().getEmail("smth").getFname("John").getLname("Doe"))

        private void asserPaymentHasBeenRegistered(){

        }
    }
}

 */